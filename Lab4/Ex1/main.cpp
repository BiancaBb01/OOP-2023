#include <iostream>
#include <stdio.h>
#include "Sort.h"

int main()
{
    Sort a(5);
    Sort *b = new Sort(5);
    ///delete b;
    bool ascendent=false;
    ///a.BubbleSort(ascendent);
   /// a.Print();
    return 0;
}
